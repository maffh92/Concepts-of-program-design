import org.scalatest.FlatSpec
import Functions.CollectObject._

class CollectTest extends FlatSpec {
  "Collect" should "get all integers from the list" in {
    //assert(collect(List(Left(1), Right(true), Left(2))) == List(1,2))
  }
}
